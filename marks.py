# Take score as input
score = int(input("Enter your score: "))

# Determine grade using if-else statements
if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
elif score >= 70:
    grade = "C"
elif score >= 60:
    g
    rade = "D"
else:
    grade = "F"

# Print the grade40
print(f"Your grade is: {grade}")
